function d(index,last,n){
	var str = String(last);
	if(index + str.length > n){
		return str[n-index];
	}

}
function main(n) {
	'use strict';
	n = parseInt(n) || 0;

	return sum;
}

console.log(main(process.argv[2]));